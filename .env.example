# Copy to .env.local for local dev. On Vercel, add these under
# Project → Settings → Environment Variables.

# Public URL of the deployed site (no trailing slash). Used for metadata,
# sitemap.xml and robots.txt.
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Web3Forms access key — get one free at https://web3forms.com using
# akinyosolaemmanuel9@gmail.com. Without it the contact form falls back
# to a pre-filled mailto: link.
NEXT_PUBLIC_WEB3FORMS_KEY=
